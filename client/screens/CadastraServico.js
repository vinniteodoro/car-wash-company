import {KeyboardAvoidingView, Text, View, Alert} from 'react-native'
import {auth, servicesRef, db} from '../configs/firebase'

export default function CadastraServicoScreen({navigation}) {

    return (
      <></>
    )
}